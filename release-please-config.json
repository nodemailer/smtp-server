{
    "packages": {
        ".": {
            "release-type": "node",
            "package-name": "smtp-server",
            "pull-request-title-pattern": "chore${scope}: release ${version}"
        }
    }
}
